Fixes:
    * Checking nonexistent path in checkBackup() loop should maybe create the path? (but that won't work for nonexistent drives)
    * Comments could use some work, maybe try doxygen standard?

Additions:
    * Catch SIGINT to gracefully stop.
